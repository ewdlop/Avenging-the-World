++       // Increment the first cell by 2 (representing 1 + 1)
> +++    // Move to the next cell and increment it by 3 (ASCII value of '3')
< .      // Move back to the first cell (value 2) and output it
